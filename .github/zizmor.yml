# zizmor configuration for tskit-dev repos.
# Third-party actions must be hash-pinned; first-party tskit-dev reusable
# workflows/actions are trusted and may stay pinned to a version tag or branch.
rules:
  unpinned-uses:
    config:
      policies:
        tskit-dev/*: ref-pin
        "*": hash-pin
